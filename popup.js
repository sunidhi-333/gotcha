// chrome.runtime.onMessage.addListener(function (message, sender, sendResponse) {
//     // const text = message.text;
//     // // Use text to fetch the related image and update the 'src' attribute of the <img> element.
//     // const relatedImage = document.getElementById('relatedImage');
//     // relatedImage.src = `URL_TO_YOUR_IMAGE_API?text=${text}`;
//         console.log("hello");

//   });

// popup.js
document.addEventListener('DOMContentLoaded', function () {
    // Add any popup-specific logic here
  });

  

  